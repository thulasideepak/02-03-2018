#include<stdio.h>
int main()
{
        int l,w,h,total;
        printf("\n enter the length :");
        scanf("%d",&l);
        printf("\n enter the width :");
        scanf("%d",&w);
        printf("\n enter the height :");
        scanf("%d",&h);
        total=l*w*h;
        printf("\n total surface and volume of cuboid:%d",total);
        return 0;
        
}

